import { RequestList } from "@/components/app/request-list";

export const metadata = {
  title: "Feature Requests — Throughline",
};

export default function RequestsPage() {
  return <RequestList />;
}
